services:
  quantgpt:
    build: .
    network_mode: host
    volumes:
      - quantgpt-data:/app/data
      - quantgpt-reports:/app/reports
    env_file:
      - .env
    environment:
      - AUTH_DISABLED=true
    restart: unless-stopped

volumes:
  quantgpt-data:
  quantgpt-reports:
